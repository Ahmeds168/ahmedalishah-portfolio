/**
 * Site-wide feature flags.
 *
 * PODCAST_ENABLED controls whether the podcast section is visible to visitors.
 * Set it to false to hide it without deleting anything: the nav link, the
 * episode pages, the sitemap entries and every cross-link from articles,
 * services and topic hubs all disappear. Set it back to true to restore them.
 *
 * Episode content in src/content/podcast/ is untouched either way.
 */
export const PODCAST_ENABLED = false;
